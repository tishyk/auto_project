
pipeline {
    agent 'any'
    stages {
        stage('Test') {
              environment {
                user = "test_user"
                password = "test_user_password"
              }
            steps {
                println("Run tests with credentials: User ${user} and Password ${password}")
                sh 'python3 --version'
            }
        }       
    }
}
